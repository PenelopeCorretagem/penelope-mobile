# SaÃ­da

- Status final: concluído

## Entregas

Os types de propriedades foram separados em arquivos próprios e a nomenclatura
de domínio foi normalizada de `Estate` para `Property`. `Advertisement` agora
expõe `property`; o campo externo da API continua sendo tratado no normalizador.

## ValidaÃ§Ãµes

`npm run typecheck` e `git diff --check` aprovados.

## PendÃªncias e prÃ³ximo passo

Sem pendências de implementação.
