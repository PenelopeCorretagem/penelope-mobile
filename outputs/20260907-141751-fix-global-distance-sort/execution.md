# ExecuÃ§Ã£o

- Status: concluído

## Alteracoes

- Exportado `sortAdvertisements` em `PropertiesModel`.
- Aplicada ordenacao global no array concatenado do ViewModel antes da paginacao.
- Corrigido fallback de `SearchModalView` de `none` para `distance`.

## Comandos e resultados

- `npm run typecheck`: aprovado.
- Busca por `sortOrder: 'none'`, `value: 'none'`, `!== 'none'` e `=== 'none'`: nenhuma referencia restante.

## Desvios, falhas e bloqueios

Nenhum.

