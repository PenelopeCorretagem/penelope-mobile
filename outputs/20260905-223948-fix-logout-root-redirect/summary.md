# Resumo

Status final: concluído

O logout encerra a autenticação imediatamente e retorna para `/`. A rota raiz decide o destino e redireciona para login quando o usuário não está autenticado.

Validação aprovada: `npm run typecheck`.
