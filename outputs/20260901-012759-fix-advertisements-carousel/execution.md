# Execṳ̣o

- Status: concluido

## Altera̵̤es

- `AdvertisementCardView` passou a aceitar largura opcional e remove a margem no modo paginado.
- `AdvertisementsCarouselView` passou a usar viewport de 100%, itens com largura da
	viewport, `pagingEnabled` e `decelerationRate="fast"`.

## Comandos e resultados

- `npm run typecheck`: passou.
- `npx expo export --platform web`: passou.

## Desvios, falhas e bloqueios

Nenhum bloqueio.
